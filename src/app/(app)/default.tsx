// Fallback nulo del route group `(app)` (dashboard / mis-cursos / perfil).
// Al ser un grupo con layout propio y sin page en la raíz, necesita default.
export default function Default() {
  return null
}
